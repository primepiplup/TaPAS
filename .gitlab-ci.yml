image: rust:slim-bookworm

stages:
    - test

testing:
    stage: test
    script:
        - cargo test
        - ./install-llvm-cov
        - cargo llvm-cov --lib --json > cov.json
        - ./coverage
    coverage: '/Total\:.*\%/'
